/**
 * Users Redirect Page
 * 
 * This page exists as a placeholder for the old /users path.
 * The user should be redirected to /dashboard/users via middleware or next.config.js
 * before this component is rendered.
 */
export default function UsersRedirectPage() {
  // This component should never be rendered as redirects should happen first
  return null;
} 