/**
 * New Workflow Redirect Page
 * 
 * This page exists as a placeholder for the old /workflows/new path.
 * The user should be redirected to /dashboard/workflows/new via middleware or next.config.js
 * before this component is rendered.
 */
export default function NewWorkflowRedirectPage() {
  // This component should never be rendered as redirects should happen first
  return null;
} 