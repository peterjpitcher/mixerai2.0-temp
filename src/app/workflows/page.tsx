/**
 * Workflows Redirect Page
 * 
 * This page exists as a placeholder for the old /workflows path.
 * The user should be redirected to /dashboard/workflows via middleware or next.config.js
 * before this component is rendered.
 */
export default function WorkflowsRedirectPage() {
  // This component should never be rendered as redirects should happen first
  return null;
} 