/**
 * Workflow Edit Redirect Page
 * 
 * This page exists as a placeholder for the old /workflows/[id]/edit path.
 * The user should be redirected to /dashboard/workflows/[id]/edit via middleware or next.config.js
 * before this component is rendered.
 */
export default function WorkflowEditRedirectPage() {
  // This component should never be rendered as redirects should happen first
  return null;
}