/**
 * Brand Detail Redirect Page
 * 
 * This page exists as a placeholder for the old /brands/[id] path.
 * The user should be redirected to /dashboard/brands/[id] via middleware or next.config.js
 * before this component is rendered.
 */
export default function BrandDetailRedirectPage() {
  // This component should never be rendered as redirects should happen first
  return null;
} 